@AGENTS.md

## Claude Code specifics

- Slash commands for this repo: `/start` (begin a session), `/save` (commit + push safely),
  `/new-indicator` (add a sub-indicator). Suggest them to the person you help.
- Dangerous git commands are blocked in `.claude/settings.json`. Do not try to work around a block.
